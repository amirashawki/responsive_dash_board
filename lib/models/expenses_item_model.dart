class ExpensesItemModel {
  final String image, title, date, price;

  ExpensesItemModel(
      {required this.image,
      required this.title,
      required this.date,
      required this.price});
}
